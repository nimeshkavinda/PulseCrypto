plugins {
  id 'com.android.library'
  id 'expo-module-gradle-plugin'
}

group = 'expo.modules.perfmonitor'
version = '1.0.0'

android {
  namespace "expo.modules.perfmonitor"
  defaultConfig {
    versionCode 1
    versionName '1.0.0'
  }
}

dependencies {
  // ReactContext / MessageQueueThread for the JS-thread frame probe.
  implementation 'com.facebook.react:react-android'
}
