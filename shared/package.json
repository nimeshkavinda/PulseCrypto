{
  "name": "@pulsecrypto/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Shared Zod schemas and TypeScript domain contracts for PulseCrypto",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --project tsconfig.test.json",
    "dev": "tsc --watch",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.6"
  }
}
